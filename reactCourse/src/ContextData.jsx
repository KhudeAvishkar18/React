import { createContext } from "react";
// createContext() is used to initiate the context api

// Here we will intiate Context API by using createContext() method
export const subjectContext = createContext('Maths');